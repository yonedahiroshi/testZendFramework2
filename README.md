# testZendFramework2

# install
```

composer create-project -sdev --repository-url="https://packages.zendframework.com" zendframework/skeleton-application zf2-tutorial

Do you want a minimal install (no optional packages)? Y/n
Y

Do you want to remove the existing VCS (.git, .svn..) history? [Y,n]? 
Y
```